/**
 * Basementen Aegis - App Version
 * Single source of truth for the version shown in the UI.
 *
 * Release checklist: bump this to match the GitHub Release tag, and bump
 * CACHE_VERSION in sw.js so installed clients pick up the new build.
 */

export const APP_VERSION = '1.1.4';
